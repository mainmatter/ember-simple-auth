{
  "name": "simple-auth-server",
  "scripts": {
    "start": "node app",
    "build": "mkdir public && cp -R $(ls | ls | grep -v '^public$') public/"
  },
  "now": {
    "name": "ember-simple-auth-server",
    "alias": "ember-simple-auth-server.now.sh"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "cors": "^2.8.1",
    "glob": "^7.1.1",
    "morgan": "^1.8.1",
    "express": "^4.15.2",
    "micro": "latest"
  }
}
