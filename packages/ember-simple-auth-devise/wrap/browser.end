global.Ember.SimpleAuth.Authenticators.Devise = requireModule('ember-simple-auth-devise');

global.Ember.SimpleAuth.initializeExtension(function(container, application, options) {
  container.register('authenticators:devise', global.Ember.SimpleAuth.Authenticators.Devise);
});
})((typeof global !== 'undefined') ? global : window);
