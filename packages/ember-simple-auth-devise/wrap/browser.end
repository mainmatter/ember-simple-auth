global.Ember.SimpleAuth.Authenticators.Devise = requireModule('ember-simple-auth-devise');

Ember.SimpleAuth.initializeExtension(function(container, application, options) {
  container.register('authenticators:devise', Ember.SimpleAuth.Authenticators.Devise);
});
})((typeof global !== 'undefined') ? global : window);
